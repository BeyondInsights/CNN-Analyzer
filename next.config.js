/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // No appDir or experimental flags needed in Next 14+
};
// const { withFirebase } = require('next-firebase-auth');