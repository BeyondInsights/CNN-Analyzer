rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Block ALL direct access to data files
    match /data/{fileName} {
      allow read, write: if false;
    }
    
    // Block everything else
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}